#ifndef LOKOTABBAR_H
#define LOKOTABBAR_H

#include <QTabWidget>
#include <QPushButton>
#include <QMessageBox>
#include <QCloseEvent>
#include <QLayout>

class LokoDock;

namespace Ui {
    class LokoTabBar;
}

class LokoTabBar : public QDialog
{
    Q_OBJECT

public:
    explicit LokoTabBar(QWidget *parent = 0);
    ~LokoTabBar();

    void closeEvent(QCloseEvent *event);

private:
    Ui::LokoTabBar *ui;

    QList<QPushButton*> buttons;
    QList<LokoDock*> docks;
    
    void setupButtons(void);

private slots:
    void onButtonClicked(void);

};

#endif // LOKOTABBAR_H
